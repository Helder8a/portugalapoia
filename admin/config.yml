# --- Backend ---
backend:
  name: git-gateway
  branch: main

# --- Media ---
media_folder: "images/uploads"
public_folder: "/images/uploads"

# --- Colección del Blog ---
collections:
  - name: "blog"
    label: "Publicaciones del Blog"
    folder: "_posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "Título del Post", name: "title", widget: "string" }
      - { label: "Data de Publicação", name: "date", widget: "datetime" }
      - { label: "Imagem de Destaque", name: "thumbnail", widget: "image", required: false }
      - { label: "Corpo do Post", name: "body", widget: "markdown", required: false, default: "" } # <-- Esta línea es la clave para el futuro